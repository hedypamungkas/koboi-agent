# === Python ===
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
*.whl
*.tar.gz

# === Virtual environments ===
.venv/
venv/
env/

# === Secrets / Environment ===
.env
.env.local
.env.*.local

# === OS metadata ===
.DS_Store
Thumbs.db
Desktop.ini

# === IDE / Editor ===
.idea/
.vscode/
*.swp
*.swo
*~

# === Testing / Coverage ===
.pytest_cache/
.coverage
.coverage.json
coverage.json
coverage.xml
htmlcov/
coverage_baseline.json
test_output.txt

# === Type checking / Linting ===
.mypy_cache/
.ruff_cache/

# === Logs ===
.logs/
*.log

# === Runtime artifacts ===
koboi_memory.db
koboi_memory.db-shm
koboi_memory.db-wal
koboi_trust.db
.agent_memory.json
.agent_memory.json.lock
.koboi_embedding_cache.json
.koboi_embedding_cache.json.tmp
.koboi_doc_cache/
# Generated deep-research findings dump (output of configs/deep_research_demo.yaml `persist_findings`)
research_findings.jsonl

# === Benchmarks (large files) ===
benchmarks/results.json
.benchmarks/

# === Test artifacts ===
MagicMock/

# === Claude Code local settings (keep rules/skills committed) ===
.claude/settings.json

# === Local analysis docs (not for commit) ===
docs/eve-impact-effort-analysis.md

# === Path B: real IR eval corpus (MS MARCO passages; research-use, built by scripts/build_ir_corpus.py) ===
data/ir_corpus/
# Native Indonesian corpus (TyDi QA; Apache-2.0, derived, regenerable via scripts/build_id_native_corpus.py)
data/id_native_corpus/
# One-off measurement/ablation corpora (built by /tmp experiment scripts: build_and_measure_id.py,
# measure_en80.py, measure_id_full.py -- NOT committed scripts; gitignored so they don't land in the repo).
data/id_corpus/
data/en80_corpus/
data/id_corpus_full/
.koboi/cache/
.koboi/workflows/
*.db
*.db-shm
*.db-wal
